<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-iseaten="{{eaten}}">
		{{#if eaten}}EATEN!{{else}}Order Another!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
